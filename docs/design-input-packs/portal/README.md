# Portal — design input packs

**Code:** `packages/portal` (portal shell, navigation, entry experiences tied to the integrated app).

**Exports:** Save DIPs and related Markdown under [`exports/`](./exports/).

When starting a Cursor session, **@ mention** the specific DIP in `exports/`.
